<!DOCTYPE html>
<html>

<head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }

        .content {
            background-color: #f9f9f9;
            padding: 20px;
            border: 1px solid #ddd;
            border-top: none;
            border-radius: 0 0 5px 5px;
        }

        .message-box {
            background-color: white;
            border: 1px solid #e0e0e0;
            border-radius: 5px;
            padding: 15px;
            margin: 15px 0;
        }

        .button {
            display: inline-block;
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
        }

        .footer {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>New Support Message</h1>
        </div>

        <div class="content">
            <p>Hello <%= @support_user.name %>,</p>

            <p>You have received a new support message from <strong><%= @sender.name %></strong>.</p>

            <div class="message-box">
                <p><strong>Message:</strong></p>
                <p><%= @message.message %></p>
            </div>

            <p><strong>Sender Details:</strong></p>
            <ul>
                <li>Name: <%= @sender.name %></li>
                <li>Email: <%= @sender.email %></li>
                <li>Role: <%= @sender.role.humanize %></li>
            </ul>

            <p><strong>Sent:</strong> <%= @message.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>

            <hr>

            <p>
                <strong>To respond to this message:</strong><br>
                1. Log in to the support dashboard: <%= root_url %>support/dashboard<br>
                2. Find "<%= @sender.name %>" in the users list<br>
                3. Click to start chatting
            </p>

            <a href="<%= root_url %>support/dashboard" class="button">Go to Support Dashboard</a>

            <div class="footer">
                <p>This is an automated message from EmbarkmentLTD Support System.</p>
                <p>Please do not reply directly to this email.</p>
            </div>
        </div>
    </div>
</body>

</html>